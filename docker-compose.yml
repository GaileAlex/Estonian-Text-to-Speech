version: '3.8'

services:
  rabbitmq:
    image: rabbitmq:3-management
    hostname: rabbitmq
    restart: unless-stopped
    ports:
      - "5672:5672"
      - "15672:15672" # Web interface: http://localhost:15672 (guest/guest)
    environment:
      - MQ_HOST=rabbitmq
      - MQ_USERNAME=guest
      - MQ_PASSWORD=guest
      - LOG_LEVEL=DEBUG
    healthcheck:
      test: ["CMD", "rabbitmqctl", "status"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - kokoro-net

  tts-worker:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    command: --model-name multispeaker --model-config /app/config/config.yaml
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
    volumes:
      - ./models:/app/models
      - ./tts-config/config.yaml:/app/config/config.yaml
    environment:
      - MQ_HOST=rabbitmq
      - MQ_USERNAME=guest
      - MQ_PASSWORD=guest
    networks:
      - kokoro-net
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]

  spring-app:
    build: .
    restart: unless-stopped
    depends_on:
      - rabbitmq
    ports:
      - "8380:8380"
    environment:
      - SPRING_RABBITMQ_HOST=rabbitmq
      - SPRING_RABBITMQ_USERNAME=guest
      - SPRING_RABBITMQ_PASSWORD=guest
    networks:
      - kokoro-net

networks:
  kokoro-net:
    external: true
